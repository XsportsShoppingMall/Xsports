* text=auto

# Specify files that should always use LF line endings, regardless of the operating system.
*.sh text eol=lf

# Specify files that should always use CRLF line endings, regardless of the operating system.
*.bat text eol=crlf

# Specify files that are binary and should not be modified.
*.png binary
*.jpg binary

./db_data text=unset